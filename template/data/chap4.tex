\chapter{Experiments and Results}

\section{Data}
The experiments use the two datasets supported by the current codebase:
MNIST and CIFAR-10. Both datasets are stored under the project-local
\path{data/} directory and are loaded through the PyTorch \texttt{torchvision}
dataset interfaces with \texttt{download=True}. No physical wireless-channel
dataset is required, because the channel perturbation is generated
synthetically by the OTA aggregation oracle.

MNIST is used as a lightweight sanity benchmark. It contains 60,000 training
images and 10,000 test images of handwritten digits. The raw files are stored
under \path{data/MNIST/raw/}. Images are converted to tensors and normalized
with mean 0.1307 and standard deviation 0.3081.

CIFAR-10 is used as the main image-classification benchmark. It contains
50,000 training images and 10,000 test images across 10 object classes. The
raw batch files \texttt{data\_batch\_1} through \texttt{data\_batch\_5},
\texttt{test\_batch}, and \texttt{batches.meta} are stored under
\path{data/cifar-10-batches-py/}. During training, CIFAR-10 uses random
cropping with padding 4 and random horizontal flipping, followed by tensor
conversion and channel-wise normalization. During testing, only tensor
conversion and normalization are applied.

Federated clients are generated by partitioning the training set. The main
comparison experiments use a non-IID Dirichlet partition with concentration
0.1 and random seed 42. In the generated partition, the MNIST comparison uses
10 clients, with client sizes ranging from 937 to 11,604 samples. The CIFAR-10
comparison uses 100 clients, with client sizes ranging from 12 to 1,733
samples. The single MNIST sanity run uses an IID split over 10 clients, with
6,000 samples per client.

\section{Experiment Configuration}
Table \ref{tab:config-main} summarizes the controlled comparison
configuration. In these experiments all four server optimizers share the same
dataset, model, partition, random seed, OTA noise setting, and training
budget. The OTA channel uses $\alpha=2$, which corresponds to AWGN in the
implemented sampler, and $\gamma=0.05$.

\begin{table}[!htbp]
    \centering
    \caption{Controlled comparison configuration}
    \label{tab:config-main}
    \begin{tabular}{p{0.34\textwidth}|p{0.26\textwidth}|p{0.26\textwidth}}
        \hline
        Item & MNIST comparison & CIFAR-10 comparison \\
        \hline
        Dataset & MNIST & CIFAR-10 \\
        Model & MLP & ResNet-18 \\
        Optimizers & FedAvg-OTA, AdaGrad-OTA, Adam-OTA & FedAvg-OTA, AdaGrad-OTA, Adam-OTA \\
        Number of clients $N$ & 10 & 100 \\
        Communication rounds & 100 & 200 \\
        Local epochs $E$ & 5 & 5 \\
        Batch size & 64 & 64 \\
        Server learning rate $\eta$ & 0.01 & 0.01 \\
        Local learning rate & 0.01 & 0.01 \\
        Momentum / $\beta_1$ & 0.9 & 0.9 \\
        Adam $\beta_2$ & 0.999 & 0.999 \\
        Adam-OTA $\varepsilon$ & $10^{-8}$ & $10^{-8}$ \\
        AdaGrad-OTA $\varepsilon$ & $10^{-4}$ & $10^{-4}$ \\
        Noise tail index $\alpha$ & 2.0 & 2.0 \\
        Noise scale $\gamma$ & 0.05 & 0.05 \\
        Partition & non-IID Dirichlet & non-IID Dirichlet \\
        Dirichlet concentration & 0.1 & 0.1 \\
        Random seed & 42 & 42 \\
        \hline
    \end{tabular}
\end{table}

Table \ref{tab:config-ablation} summarizes the revised CIFAR-10 heavy-tail
ablation configuration. The tail-index ablation varies $\alpha$ while fixing
$\gamma=0.05$. The MAC comparison uses the same heavy-tailed channel with
$\alpha=1.3$ and compares training with and without robust pre-processing.
The learning-rate sweep holds the same $\alpha=1.3$ channel fixed and varies
the server learning rate for AdaGrad-OTA and Adam-OTA.

\begin{table}[!htbp]
    \centering
    \caption{CIFAR-10 ablation configuration}
    \label{tab:config-ablation}
    \begin{tabular}{p{0.36\textwidth}|p{0.46\textwidth}}
        \hline
        Item & Value \\
        \hline
        Dataset / model & CIFAR-10 / ResNet-18 \\
        Optimizers in alpha study & FedAvg-OTA,AdaGrad-OTA,\\
        & Adam-OTA \\
        Alpha values & 1.1, 1.3, 1.5, 1.7, 1.9, 2.0 \\
        MAC comparison & no MAC vs MAC, $k=3$ \\
        Learning-rate sweep & AdaGrad-OTA / Adam-OTA at $\alpha=1.3$,
        $\eta\in$$\{0.0001,0.0003,0.001,0.003,0.01,0.03,0.1\}$ \\
        Communication rounds & 100 \\
        Local epochs $E$ & 1 \\
        Batch size & 64 \\
        Server learning rate $\eta$ & 0.001 \\
        Local learning rate & 0.01 \\
        Momentum / $\beta_1$ & 0.9 \\
        Adam $\beta_2$ & 0.999 \\
        Adam-OTA $\varepsilon$ & $10^{-8}$ \\
        AdaGrad-OTA $\varepsilon$ & $10^{-4}$ \\
        Noise scale $\gamma$ & 0.05 \\
        Partition & non-IID Dirichlet, concentration 0.1 \\
        Random seeds & 42, 43, 44 \\
        \hline
    \end{tabular}
\end{table}

\section{Evaluation Metrics}
The main metrics are test loss and test accuracy after each communication
round. For comparison experiments, the final-round accuracy and the best
accuracy achieved during the run are both reported. The final value measures
the state of the trained model at the end of the fixed communication budget,
while the best value helps reveal short-lived convergence peaks.

For the alpha ablation, the reported metric is final test accuracy under each
tail index $\alpha$. For the learning-rate sweep, the reported metric is final
test accuracy for each server learning rate. For the MAC comparison, the
reported metric is final test accuracy with and without clipping. Multi-seed
results are reported as mean $\pm$ standard deviation. All revised heavy-tail
values are generated from the JSON result files rather than copied from a
single run.

\section{Main Method Comparison}
The MNIST comparison in Table \ref{tab:mnist-results} is used as a sanity
benchmark for adaptive server-side normalization. FedAvg-OTA reaches 67.85\%
final accuracy, while AdaGrad-OTA and
Adam-OTA reach 93.05\% and 93.46\%, respectively. This supports the
hypothesis that second-moment adaptation can stabilize OTA training in this
lighter benchmark, but it should not be treated as the main heavy-tail
evidence.

\begin{table}[!htbp]
    \centering
    \caption{MNIST comparison results ($N{=}10$, AWGN $\alpha{=}2$,
    $\gamma{=}0.05$, seed 42, 100 rounds, $E{=}5$,
    $\eta{=}0.01$; single seed, no error bar).}
    \label{tab:mnist-results}
    \begin{tabular}{l|c|c|c}
        \hline
        Method & Final loss & Final accuracy & Best accuracy \\
        \hline
        FedAvg-OTA & 1.5018 & 67.85\% & 67.85\% \\
        AdaGrad-OTA & 0.2091 & 93.05\% & 93.19\% \\
        Adam-OTA & 0.2227 & 93.46\% & 93.46\% \\
        \hline
    \end{tabular}
\end{table}

The CIFAR-10 comparison in Table \ref{tab:cifar-results} is more nuanced.
AdaGrad-OTA and Adam-OTA improve over plain
FedAvg-OTA under this specific
CIFAR-10 configuration. Therefore, the CIFAR-10 result should not be stated as
``Adam-OTA is always best''. A more accurate conclusion is that adaptive
methods help relative to FedAvg-OTA in this setting.
This AWGN endpoint motivates the heavier $\alpha$-stable experiments below:
Gaussian noise alone is not the strongest stress test, so the project's
distinctive question is whether adaptive normalization and MAC help when the
channel has impulsive tails.

\begin{table}[!htbp]
    \centering
    \caption{CIFAR-10 comparison results ($N{=}100$, AWGN
    $\alpha{=}2$, $\gamma{=}0.05$, seed 42, 200 rounds,
    $E{=}5$, $\eta{=}0.01$; single seed, no error bar).}
    \label{tab:cifar-results}
    \begin{tabular}{l|c|c|c}
        \hline
        Method & Final loss & Final accuracy & Best accuracy \\
        \hline
        FedAvg-OTA & 0.9670 & 65.38\% & 65.38\% \\
        AdaGrad-OTA & 0.8706 & 69.27\% & 69.27\% \\
        Adam-OTA & 0.9278 & 67.41\% & 68.08\% \\
        \hline
    \end{tabular}
\end{table}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{../results/figures/cifar10_resnet18_alpha2.0_N100_curves.png}
    \caption{CIFAR-10 comparison curves for FedAvg-OTA,
    AdaGrad-OTA, and Adam-OTA under AWGN. The comparison focuses on the
    plain FedAvg baseline and the two adaptive server optimizers.}
    \label{fig:cifar-curves}
\end{figure}

The single-run outputs provide an additional sanity check, but they are not a
controlled four-method comparison because their hyperparameters differ from
Table \ref{tab:config-main}. The single CIFAR-10 Adam-OTA run uses batch size
128 and server learning rate 0.01, reaching 61.51\% final accuracy. The
single MNIST AdaGrad-OTA run uses an IID client split and reaches 97.57\%
final accuracy. These runs confirm that the implemented optimizers can train
successfully, but the main conclusions should be drawn from the controlled
comparison tables above.

\section{Alpha-Stable Tail-Index Ablation}
The revised core experiment varies the $\alpha$-stable tail index instead of
only varying the AWGN scale. The endpoint $\alpha=2$ is Gaussian AWGN, while
smaller values of $\alpha$ produce heavier-tailed impulsive interference.
This is the setting where adaptive normalization is most meaningful: a
server method must handle rare but very large aggregate perturbations.
The adaptive methods complete the full 100 communication rounds for all three
seeds. In contrast, FedAvg-OTA often diverges under the
heaviest-tailed settings; the generated table therefore reports the number of
successful seeds rather than silently averaging failed runs.

\IfFileExists{data/generated/alpha_ablation_table.tex}{
    \input{data/generated/alpha_ablation_table}
}{
    \begin{table}[!htbp]
        \centering
        \caption{CIFAR-10 $\alpha$-stable tail-index ablation}
        \label{tab:alpha-ablation}
        \begin{tabular}{c|c}
            \hline
            Result file & Status \\
            \hline
            \path{results/heavytail1/alpha_ablation_cifar10_resnet18.json}
            & Run \path{./run_all.sh alpha_ablation} and then
            \path{./run_all.sh paper_update} \\
            \hline
        \end{tabular}
    \end{table}
}

\begin{figure}[!htbp]
    \centering
    \IfFileExists{../results/figures/alpha_ablation_cifar10_resnet18_plot.png}{
        \includegraphics[width=0.9\textwidth]{../results/figures/alpha_ablation_cifar10_resnet18_plot.png}
    }{
        \fbox{\parbox{0.85\textwidth}{\centering
        Alpha-ablation figure is generated by
        \path{./run_all.sh paper_update} after the heavy-tail runs finish.}}
    }
    \caption{CIFAR-10 final accuracy as a function of $\alpha$-stable tail index $\alpha$.}
    \label{fig:alpha-ablation}
\end{figure}

Table \ref{tab:alpha-ablation} shows a clear stability separation between
methods. FedAvg-OTA produces no successful runs for
$\alpha\leq1.9$ under the finite-value guard. At the Gaussian endpoint
$\alpha=2$, the baseline completes all seeds and reaches 21.43\% mean final
accuracy. In contrast, both adaptive methods complete all seeds
for every tested $\alpha$. AdaGrad-OTA improves from 22.12\% at
$\alpha=1.1$ to 35.35\% at $\alpha=1.9$ and 36.67\% at $\alpha=2$.
Adam-OTA improves from 28.64\% at $\alpha=1.1$ to 39.65\% at
$\alpha=1.9$ and 39.74\% at $\alpha=2$.

The result supports the core robustness claim: server-side fractional
second-moment normalization makes the implemented OTA-FL loop much less
fragile under impulsive $\alpha$-stable noise than FedAvg-style updates.
It also matches the reference paper's qualitative tail-index message:
heavier tails make training harder, and performance generally improves as
$\alpha$ approaches the Gaussian endpoint. The result is still narrower than
a full reproduction of \cite{wang2024adaptive}. The implementation omits
fading and power control, uses a shared $\alpha$ for both the channel and the
adaptive denominator, and uses a single global server learning rate rather
than per-method tuning in this table. The next section runs a targeted
learning-rate sweep to test how sensitive the $\alpha=1.3$ comparison is to
the shared learning-rate choice.

\section{Server Learning-Rate Sweep}
\label{sec:lr-sweep}
The tail-index ablation uses a single server learning rate,
$\eta=10^{-3}$, for all methods. To check sensitivity to this shared
learning-rate choice at $\alpha=1.3$, we rerun AdaGrad-OTA and Adam-OTA on
the same CIFAR-10 / ResNet-18 setting over seven learning rates from
$10^{-4}$ to $10^{-1}$.

\IfFileExists{data/generated/lr_sweep_table.tex}{
    \input{data/generated/lr_sweep_table}
}{
    \begin{table}[!htbp]
        \centering
        \caption{CIFAR-10 server learning-rate sweep under heavy-tailed interference}
        \label{tab:lr-sweep}
        \begin{tabular}{c|c}
            \hline
            Result file & Status \\
            \hline
            \path{results/heavytail/lr_sweep_cifar10_resnet18_alpha1.3.json}
            & Run \path{./run_all.sh lr_sweep} and generate the table with
            \path{src/experiments/report.py} \\
            \hline
        \end{tabular}
    \end{table}
}

\begin{figure}[!htbp]
    \centering
    \IfFileExists{../results/figures/lr_sweep_cifar10_resnet18_alpha1.3_plot.png}{
        \includegraphics[width=0.9\textwidth]{../results/figures/lr_sweep_cifar10_resnet18_alpha1.3_plot.png}
    }{
        \fbox{\parbox{0.85\textwidth}{\centering
        Learning-rate sweep figure is generated by
        \path{./run_all.sh lr_sweep}.}}
    }
    \caption{CIFAR-10 final accuracy as a function of server learning rate at
    $\alpha=1.3$, $\gamma=0.05$.}
    \label{fig:lr-sweep}
\end{figure}

Table \ref{tab:lr-sweep} and Figure \ref{fig:lr-sweep} show that the adaptive
methods prefer different server learning rates. AdaGrad-OTA is best at the
default $\eta=10^{-2}$, reaching 34.62\% $\pm$ 0.64\% final accuracy, which
matches the 34.56\% result in the tail-index table up to rerun variation.
Adam-OTA is best one decade lower, at $\eta=10^{-3}$, where it reaches
33.65\% $\pm$ 0.85\%. This is a 6.95 percentage-point gain over the
$\eta=10^{-2}$ row in the same sweep, and a 6.74 percentage-point gain over
the default Adam-OTA result in Table~\ref{tab:alpha-ablation}.
The sweep therefore shows that Adam-OTA's lower default accuracy is partly a
learning-rate mismatch, but the tested grid does not reverse the ranking:
AdaGrad-OTA remains slightly ahead at its own best learning rate. Both
methods fail the finite-value guard at $\eta=10^{-1}$, confirming that the
adaptive denominator does not make arbitrarily large server steps safe.

\section{MAC Robust Pre-Processing}
Median Anchored Clipping (MAC) is a robust pre-processing technique
proposed by Li et al.~\cite{li2024robustmac} specifically for OTA-FL
under heavy-tailed $\alpha$-stable channel noise. It operates on the
server-side aggregated gradient $\tilde{g}^t$ before the optimizer step:
(1)~compute the coordinate-wise median~$m$, (2)~center the gradient
as $\tilde{g}^t - m$, (3)~clip each centered coordinate to
$[-k\cdot\mathrm{MAD},\,+k\cdot\mathrm{MAD}]$ where
$\mathrm{MAD}=\mathrm{median}(|\tilde{g}^t-m|)$, and (4)~restore the
median. Because the median is a robust statistic, MAC is far less
sensitive to extreme outliers than gradient-norm clipping and preserves
the proportional relationships among gradient
dimensions~\cite{li2024robustmac}.

MAC and adaptive server optimizers interact through two opposing effects.
On one hand, MAC removes impulsive coordinates before the optimizer sees
them, whereas AdaGrad-OTA and Adam-OTA already use the magnitude of
$\tilde{g}^t$ to grow a fractional second-moment accumulator and shrink the
effective step size. On the other hand, when raw impulses are large enough
to cause overflow or very poor early updates, clipping can keep the
aggregate stream finite and make adaptive normalization easier to use.
For the non-adaptive FedAvg-OTA baseline, the expected benefit is larger
because the method lacks any built-in outlier shrinkage.

To test this interaction empirically, we compare training with and without MAC
under a heavy-tailed setting. The default comparison uses
$\alpha=1.3$, $\gamma=0.05$, and clipping factor $k=3$. MAC is not expected
to change the Gaussian endpoint dramatically, but it should reduce the
effect of rare impulsive coordinates when $\alpha<2$.

\IfFileExists{data/generated/mac_compare_table.tex}{
    \input{data/generated/mac_compare_table}
}{
    \begin{table}[!htbp]
        \centering
        \caption{CIFAR-10 MAC comparison under heavy-tailed interference}
        \begin{tabular}{c|c}
            \hline
            Result file & Status \\
            \hline
            \path{results/heavytail1/mac_compare_cifar10_resnet18_alpha1.3.json}
            & Run \path{./run_all.sh mac_compare} and then
            \path{./run_all.sh paper_update} \\
            \hline
        \end{tabular}
    \end{table}
}

\begin{figure}[!htbp]
    \centering
    \IfFileExists{../results/figures/mac_compare_cifar10_resnet18_alpha1.3_plot.png}{
        \includegraphics[width=0.9\textwidth]{../results/figures/mac_compare_cifar10_resnet18_alpha1.3_plot.png}
    }{
        \fbox{\parbox{0.85\textwidth}{\centering
        MAC comparison figure is generated by
        \path{./run_all.sh paper_update} after the heavy-tail runs finish.}}
    }
    \caption{CIFAR-10 comparison of no-MAC and MAC under $\alpha$-stable interference.}
    \label{fig:mac-compare}
\end{figure}

In the MAC comparison, clipping creates a clear benefit at this
operating point. Without MAC, FedAvg-OTA has 0/3 successful runs under the
finite-value guard, and this failed condition is reported at chance accuracy 
10.00\%. With MAC, all three FedAvg-OTA seeds complete and reach
20.34\% $\pm$ 1.37\% final accuracy. The adaptive methods also improve:
AdaGrad-OTA increases from 25.29\% $\pm$ 0.88\% to
28.02\% $\pm$ 1.00\%, and Adam-OTA increases from
33.87\% $\pm$ 0.73\% to 38.84\% $\pm$ 0.90\%. These results show that,
for the guarded $\alpha=1.3$, $\gamma=0.05$, $k=3$ run, MAC is not merely
a neutral pre-processor; it can prevent fixed-step divergence and further
improve adaptive training. The interpretation remains operating-point
dependent because the clipping factor, noise scale, and server learning
rate jointly determine whether MAC removes harmful impulses or useful
gradient signal. The \texttt{mac\_sweep} extension in
\path{src/experiments/heavy_tail.py} is therefore the right next tool for
mapping when this benefit persists.

\section{Client-Count Ablation}
Table \ref{tab:client-ablation} reports the Adam-OTA client-count
ablation on CIFAR-10. With the same 100-round budget, increasing the number of
clients improves final accuracy from 10.26\% at 5 clients to 21.72\% at 100
clients. This trend is consistent with OTA aggregation: averaging more client
updates can make the aggregate direction more informative, even though the
data remain non-IID and the channel still injects noise.

This ablation uses the configuration in
\path{src/experiments/ablation.py} (CIFAR-10, ResNet-18, AWGN $\alpha=2$,
$\gamma=0.05$, 100 rounds, local epochs $E=1$, batch size 64,
server learning rate $\eta=10^{-4}$, local learning rate $0.01$,
$\beta_1=0.9$, $\beta_2=0.999$, non-IID Dirichlet 0.1, seed 42). The much
smaller server learning rate, smaller local epochs, and shorter budget
explain why the absolute Adam-OTA accuracies here are below
Table~\ref{tab:cifar-results}, which uses $\eta=0.01$, $E=5$, and 200
rounds. The ablation isolates the effect of $N$ rather than tuning a
state-of-the-art Adam-OTA configuration.

\begin{table}[!htbp]
    \centering
    \caption{CIFAR-10 client-count ablation with Adam-OTA
    ($\eta=10^{-4}$, $E=1$, 100 rounds, $\alpha=2$, $\gamma=0.05$,
    $\beta_2=0.999$, non-IID Dirichlet 0.1, seed 42)}
    \label{tab:client-ablation}
    \begin{tabular}{c|c|c|c}
        \hline
        Clients $N$ & Final loss & Final accuracy & Best accuracy \\
        \hline
        5 & 2.3121 & 10.26\% & 10.36\% \\
        10 & 2.2288 & 16.92\% & 16.96\% \\
        20 & 2.1249 & 19.99\% & 19.99\% \\
        50 & 2.1171 & 20.36\% & 20.36\% \\
        100 & 2.0880 & 21.72\% & 21.72\% \\
        \hline
    \end{tabular}
\end{table}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{../results/figures/ablation_clients_cifar10_resnet18_plot.png}
    \caption{CIFAR-10 client-count learning curves for Adam-OTA.}
    \label{fig:client-ablation}
\end{figure}

\paragraph{Why does $N$ not scale accuracy faster?}
In the implemented oracle, additive receiver noise is injected after the
client updates are summed and then divided by $N\eta_{\rm loc}$, so the
channel-noise term decreases as $1/N$ in this simulator. The accuracy gain is
nevertheless modest because increasing $N$ also changes the non-IID client
partition, reduces the average data per client, and keeps the same small
server learning rate and 100-round budget. The client-count ablation should
therefore be interpreted as a software sanity check that larger participation
helps, not as a physical-layer scaling law.

\section{Discussion}

\subsection{Findings}
The experiments should be interpreted in two layers. The existing AWGN
comparison remains a useful endpoint sanity check: it shows that the
implementation trains successfully. The revised $\alpha$-stable
ablation is the central evidence for the project claim, because it
tests the heavy-tailed channel condition that motivates adaptive
OTA-FL in the first place.

The project conclusion should therefore be phrased carefully. The
results do \emph{not} prove that Adam-OTA universally dominates all
baselines. Instead, the claim is that fractional second-moment adaptive
updates provide a practical mechanism for improving OTA-FL stability
when the aggregate update is corrupted by $\alpha$-stable impulsive
noise, and MAC can provide a complementary robustness lever when raw
impulses would otherwise destabilize the server update.

\subsection{Failures and Negative Results}
Three observations are best reported as negative results rather than
buried in caveats. (i) Adam-OTA does not universally dominate AdaGrad-OTA:
although it is stronger in the current tail-index ablation, the targeted
learning-rate sweep still finds an AdaGrad-OTA setting with slightly higher
final accuracy at $\alpha=1.3$. (ii) FedAvg-OTA without MAC remains highly
fragile in the heavy-tailed setting: in the updated $\alpha=1.3$ MAC
comparison it has 0/3 successful no-MAC runs and is reported at chance
accuracy, so any fixed-step baseline claim must report the failure guard
rather than only partial accuracies.
(iii) The Adam-OTA CIFAR-10
result at $\alpha=2$ in
Table~\ref{tab:cifar-results} (67.41\%) is above FedAvg-OTA (65.38\%) but
below AdaGrad-OTA (69.27\%), showing that second-moment normalization is not
costless in the Gaussian regime.

\subsection{Threats to Validity}
\label{sec:threats-validity}
The empirical evidence is bounded by four threats. First, seed counts
differ across tables (three seeds for the $\alpha$ sweep and MAC
comparison, single seed for the main comparison and client-count
ablation); we note this in the captions rather than backfill all
tables. Second, the learning-rate sweep is targeted rather than exhaustive:
it covers only AdaGrad-OTA and Adam-OTA at $\alpha=1.3$, so broader optimizer
rankings across all $\alpha$ values, $\beta_2$ values, and baselines remain
tuning-dependent. Third, the channel is a simulator, without fading, power
control, or synchronization errors. Fourth, the strongest supported claim is
empirical; the finite-trace stability analysis in
\S\ref{sec:finite-trace-stability} explains the implemented
normalization mechanism but is not a full non-convex proof.
